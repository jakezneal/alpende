{
    "name": "nuxt-app",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "nuxt build",
        "dev": "nuxt dev",
        "generate": "nuxt generate",
        "preview": "nuxt preview",
        "postinstall": "nuxt prepare",
        "format": "prettier --write \"**/*.{vue,md,json}\""
    },
    "dependencies": {
        "nuxt": "^3.12.4",
        "vue": "latest"
    },
    "devDependencies": {
        "prettier": "3.3.3"
    }
}
